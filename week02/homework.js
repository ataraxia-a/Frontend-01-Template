// 1. 正则匹配数字字面量
const regNumber = /^([\+ \-]?(([1-9]\d*)|(0)))([.]\d{0,2})?$/;

// TODO: 2. UTF-8 Encoding function

// TODO: 3. 正则表达式，匹配所有的字符串直接量，单引号和双引号

const regString = /(\\'.*?\\')|(\\".*?\\")/;
